#ifndef ELEVATOR_H
#define ELEVATOR_H

#include <QObject>
#include <QTimer>
class elevator : public QObject
{
    Q_OBJECT
private:
    enum{
        MOVE_UP,
        MOVE_DOWN,
        STOP
    } state;
    QTimer timer;
public:
    elevator();
    
signals:
    void signals_stop();
private slots:
    void slots_stop();
public slots:
    void slots_up();
    void slots_down();
};

#endif // ELEVATOR_H
